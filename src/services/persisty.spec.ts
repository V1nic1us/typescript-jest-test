describe('Persisty 1', () => {
  it('should return ten', () => {
    const number = 10;
    expect(number).toBe(10);
  });
});

describe('Persisty 2', () => {
  test('should return Marcus', () => {
    const number = 'Marcus';
    expect(number).toBe('Marcus');
  });
});
